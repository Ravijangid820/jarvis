# Shell scripts must keep LF endings even when checked out on Windows — otherwise bash inside the
# Linux container fails with "$'\r': command not found". (The Dockerfile also strips CR as a belt.)
*.sh text eol=lf
