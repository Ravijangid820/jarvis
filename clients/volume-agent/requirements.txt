# Jarvis volume agent (Windows). Install on the laptop:  pip install -r requirements.txt
# For a tamper-evident install, compile to a hash-pinned lockfile (pip install --require-hashes).
pycaw>=20230407
comtypes>=1.2,<2
