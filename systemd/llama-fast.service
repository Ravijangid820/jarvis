[Unit]
Description=Jarvis Fast Brain - Qwen3.5-2B LLM Server
After=network.target
Documentation=https://github.com/ggml-org/llama.cpp
StartLimitIntervalSec=120
StartLimitBurst=5

[Service]
Type=simple
ExecStart=/root/llama.cpp/build/bin/llama-server \
    -m /srv/jarvis/models/qwen3.5_2b/Qwen3.5-2B-Q4_K_M.gguf \
    -c 4096 \
    -t 2 \
    --batch-size 256 \
    --ubatch-size 256 \
    --host 127.0.0.1 \
    --port 8081 \
    --parallel 1 \
    --reasoning off
Restart=on-failure
RestartSec=10
SyslogIdentifier=llama-fast
# Security hardening
# NOTE: ProtectHome is not set because the llama-server binary lives in /root/llama.cpp.
# It serves only loopback (127.0.0.1:8081) and loads a local GGUF — lower risk than the
# orchestrator, but still root; moving the build out of /root to run non-root is a follow-up.
NoNewPrivileges=true
PrivateTmp=true
PrivateDevices=true
ProtectSystem=full
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectControlGroups=true
RestrictSUIDSGID=true
RestrictAddressFamilies=AF_INET AF_INET6
LockPersonality=true
UMask=0077
# Resource limits
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
