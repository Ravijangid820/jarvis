# Jarvis Camera — Raspberry Pi deps. SEPARATE from the server's pyproject (different machine/arch).
# Pi 3 B+ notes: use a 64-bit OS (needed for mediapipe wheels) and add a swapfile (only 1 GB RAM).
#
# Install OpenCV + picamera2 via apt (lighter/faster than building pip wheels on a Pi); see setup.sh:
#   sudo apt install -y python3-opencv python3-picamera2 libatlas-base-dev
# The venv is created with --system-site-packages so the apt-installed cv2/picamera2 are visible.

# Bounded ranges (avoid silently pulling a future major). For a tamper-evident install,
# compile these to a hash-pinned lockfile and use `uv pip install --require-hashes`.
numpy<2
requests>=2.31,<3

# Faces (YuNet detect + SFace recognize) run via cv2 — no extra deps beyond OpenCV. The two ONNX
# models are downloaded + sha256-verified by setup.sh into camera/models/.
#
# Heavy / optional — only for pose + hand gestures (faces do NOT need this; Pi 3: expect a few FPS):
# mediapipe>=0.10,<0.11    # MediaPipe Pose + Hands; 64-bit OS only, memory-tight on 1 GB
