# Runtime config + device API key (never commit); keep config.example.json at camera/ root.
config/
.venv/
models/          # ONNX models are downloaded + sha256-verified by setup, not committed
dist/            # PyInstaller .exe output (built by CI)
build/           # PyInstaller scratch
__pycache__/
*.pyc
